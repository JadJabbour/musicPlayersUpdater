/**
 * Configuration object for the tool
 */
const Configuration = {
    /** URL for the update API */
    update_api_url: "",
    /** URL for the authentication server to get the token */
    auth_server_url: ""
}

export default Configuration;